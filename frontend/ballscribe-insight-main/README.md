# CrickTrack — Frontend

React + Vite + TypeScript frontend for CrickTrack, an AI cricket ball tracking and analysis tool.

## Getting started

```bash
npm install
npm run dev
```

The dev server runs on http://localhost:8080 by default and talks to the backend API at http://localhost:8000.
